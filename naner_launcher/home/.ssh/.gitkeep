# This directory contains portable SSH configuration
#
# Place your SSH keys and config files here for a portable SSH setup.
# The .ssh directory will be preserved in git, but sensitive files are gitignored.
